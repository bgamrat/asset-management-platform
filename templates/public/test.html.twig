{% block title %}{{ 'common.test'|trans }}{% endblock %}
{% extends '/admin.base.html.twig' %}


{#% form_theme test_form _self %}

{% block money_widget %}
    Money Widget
    {{dump(form)}}
    {{form}}
{% endblock %#}

{% block admin_content %}

    <h1>{{ block ('title') }}</h1>

    <button id="test-btn">Test</button>

    {{form_start(test_form)}}
    {{form_widget(test_form.money)}}
    {{form_end(test_form)}}

{% endblock %}
{% block page_js %}
    {% if app.environment == 'prod' %}
        <script src="/release/app/test.js"></script>
    {% endif %}
    <script>
        require(["app/test",
            "dojo/domReady!"], function (test) {
            test.run();
        });
    </script>
{% endblock %}