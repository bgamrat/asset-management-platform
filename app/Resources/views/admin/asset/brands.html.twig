<div class="brands" id="manufacturer_brands_div">
    <span class="sub-form-legend">{{'asset.brands'|trans}}</span>
    {{form_row(form)}}
    {% if form.vars.allow_add %}
    <div class="add-one-more-row">{{ 'common.add_one_more'|trans}}</div>
    {% endif %}
</div>